# Template Sync workflow for hvd modules. This workflow is invoked from workflows in the hvd module repos.
name: Template Sync

on:
  workflow_call:
permissions:
  contents: write
  pull-requests: write

jobs:
    repo-sync:
      runs-on: ubuntu-latest
      permissions:
        contents: write
        pull-requests: write

      steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: actions-template-sync
        uses: AndreasAugustin/actions-template-sync@8a0f668b83c32a0f673353086d74f12b8853d4f5 # v2.5.3
        with:
          source_repo_path: hashicorp/hvd-module-template
          upstream_branch: main
          pr_labels: template_sync

